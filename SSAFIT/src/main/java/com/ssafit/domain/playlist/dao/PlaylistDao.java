package com.ssafit.domain.playlist.dao;

import java.util.List;

import org.apache.ibatis.annotations.Param;

import com.ssafit.domain.playlist.entity.Playlist;

public interface PlaylistDao {

    // ============================================================
    // C: 플레이리스트 생성
    // ============================================================
    int insertPlaylist(Playlist playlist);

    // ============================================================
    // R: 플레이리스트 단건 조회
    // ============================================================
    Playlist selectById(@Param("id") Long id);

    // ============================================================
    // R: 내가 만든 플레이리스트 목록 조회
    // ============================================================
    List<Playlist> selectByPublisherId(@Param("publisherId") Long publisherId);

    // ============================================================
    // R: 전체 플레이리스트 조회 (공개용/탐색용)
    // ============================================================
    List<Playlist> selectAll();

    // ============================================================
    // U: 플레이리스트 수정
    // ============================================================
    int updatePlaylist(Playlist playlist);

    // ============================================================
    // D: 플레이리스트 삭제
    // ============================================================
    int deletePlaylist(@Param("id") Long id);
}
