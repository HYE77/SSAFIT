<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafit.domain.board.dao.CommentDao">

    <insert id="insertComment" parameterType="Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (
            post_id, user_id, content,
            like_cnt, deleted
        )
        VALUES (
            #{postId}, #{userId}, #{content},
            0, FALSE
        )
    </insert>

    <select id="selectById" resultType="Comment">
        SELECT * FROM comment
        WHERE id = #{id}
    </select>

    <select id="selectByPostId" resultType="Comment">
        SELECT *
        FROM comment
        WHERE post_id = #{postId}
        ORDER BY created_at ASC
    </select>

    <update id="updateComment" parameterType="Comment">
        UPDATE comment
        SET content = #{content}
        WHERE id = #{id}
    </update>

    <update id="softDeleteComment">
        UPDATE comment
        SET deleted = TRUE
        WHERE id = #{id}
    </update>

</mapper>
